[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
boto3 = "*"
docker = "*"
pyyaml = "*"
requests = "*"

[scripts]
test = "pipenv run python deployment_test.py"
version = "python setup.py sdist bdist_wheel"
upload = "twine upload --verbose --skip-existing dist/*"
